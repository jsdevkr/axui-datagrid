import * as React from 'react';
import { typeStore } from '../stores';

interface IProps {}
interface IState {}

class DatagridColumnFilterOption extends React.Component<IProps, IState> {
  constructor(props: any) {
    super(props);
  }

  render() {
    return <div />;
  }
}

export default DatagridColumnFilterOption;
